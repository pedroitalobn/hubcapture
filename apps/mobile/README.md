# Hub Capture — Mobile (Expo)

**Fase 2.** Scaffold apenas. O app consome a MESMA API v1 (FastAPI) que o web,
via client tipado gerado do OpenAPI (`@hub/api-client`). Nenhum backend é
reescrito quando o app entrar. Ver seção 10 de `docs/ARQUITETURA_HUB_CAPTURE.md`.
