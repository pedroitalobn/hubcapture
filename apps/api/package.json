{
  "name": "@hub/api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn src.main:app --reload --host 0.0.0.0 --port 8000",
    "test": "uv run pytest",
    "lint": "uv run ruff check .",
    "generate": "uv run python -m src.export_openapi",
    "migrate": "uv run alembic upgrade head"
  }
}
