// Preset Tailwind compartilhado (Tailwind 4). Web estende este preset.
/** @type {import('tailwindcss').Config} */
module.exports = {
  theme: {
    extend: {
      colors: {
        brand: {
          DEFAULT: "#1d4ed8",
          fg: "#ffffff",
        },
      },
    },
  },
  plugins: [],
};
